[build]
builder = "NIXPACKS"

[deploy]
startCommand = "php artisan migrate --force; php-fpm82 -D && nginx -g 'daemon off;'"
healthcheckPath = "/"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
